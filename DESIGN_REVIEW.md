# 시안 검토 체크리스트

Figma 시안을 받은 날, 퍼블리싱을 시작하기 전에 디자이너·기획자·개발사와 답을 확정한다. 각 질문의 답은 Figma 코멘트나 이슈 번호로 남기고, “퍼블리셔 판단”으로 비워 두지 않는다. 중간에 답이 바뀌면 변경일과 영향 페이지도 기록한다.

## 상태와 컴포넌트

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 버튼·링크·입력·탭·카드마다 `hover`, `focus`, `active`, `disabled`, `loading`, `error`, `selected` 상태가 모두 정의되어 있는가? | 기본 화면만 보고 구현하면 QA 때 상태별 색·문구·동작을 다시 설계하게 된다. 특히 focus는 키보드 사용 가능 여부를 결정한다. | |
| disabled와 loading에서 클릭·submit을 막는가, 진행 중 문구와 `aria-busy`는 무엇인가? | 중복 주문이나 중복 결제를 막고 보조기술에도 처리 중임을 알려야 한다. | |
| 오류는 필드 단위인가, 폼 상단 요약도 필요한가? 오류 문구와 해제 시점은 무엇인가? | 색 테두리만으로는 원인과 복구 방법을 전달할 수 없고 서버 오류와 입력 오류의 위치도 다르다. | |
| 선택됨·품절·할인·필수 상태가 색상 외 텍스트나 아이콘 형태로도 보이는가? | 색각 차이와 흑백 환경에서도 상태가 구분되어야 한다. | |
| 모달·바텀시트·토스트의 열림/닫힘 조건, 중첩 허용 여부, 자동 닫힘 시간이 정의되어 있는가? | 포커스 복귀와 알림 읽기 시간, 뒤로가기 처리 방식이 상태 명세에 따라 달라진다. | |

## 빈 상태와 예외 흐름

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 검색 결과 없음 화면의 제목, 설명, 검색어 초기화/추천 행동은 무엇인가? | 결과 개수 0만 표시하면 사용자가 다음 행동을 알 수 없다. | |
| 장바구니가 비었을 때 CTA와 추천 영역은 무엇인가? | 결제 요약만 숨기면 남는 레이아웃과 구매 여정이 정의되지 않는다. | |
| 주문 내역·리뷰·쿠폰이 없을 때 각각 어떤 문구와 행동을 제공하는가? | “데이터 없음” 하나로 통일하면 영역의 목적과 다음 행동이 사라진다. | |
| 목록 로딩, 부분 실패, 전체 실패, 재시도 상태가 각각 있는가? | 느린 망과 API 실패를 빈 목록으로 오인하면 사용자가 데이터가 없는지 장애인지 구분하지 못한다. | |
| 이미지 로드 실패와 상품 판매 종료 시 대체 이미지·문구·링크 정책은 무엇인가? | 외부 CDN과 상품 상태는 정상 화면과 독립적으로 실패할 수 있다. | |

## 콘텐츠 극단값

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 상품명이 2~4줄 이상일 때 말줄임인가, 전체 노출인가? 카드 높이는 맞추는가? | 실제 상품명은 시안 샘플보다 길며 카드 정렬과 접근 가능한 이름이 달라진다. | |
| 가격이 8자리 이상이고 할인율·정가·판매가가 함께 있을 때 줄바꿈 규칙은 무엇인가? | 큰 금액과 통화 표기가 CTA나 수량 영역을 밀어낼 수 있다. | |
| 리뷰가 0개일 때 별점·평균·리뷰 링크는 어떻게 보이는가? | 0점을 낮은 평가로 오해하지 않게 “아직 리뷰 없음” 상태를 분리해야 한다. | |
| 옵션이 20개 이상이거나 옵션명이 매우 길 때 select, 목록, 바텀시트의 최대 높이와 스크롤은 무엇인가? | 옵션 UI가 화면을 넘어가면 구매 버튼과 닫기 수단에 도달하지 못한다. | |
| 장바구니·주문 목록이 수십 건일 때 요약 영역은 고정인가, 문서 흐름인가? | 긴 목록에서 성능, 키보드 이동, 고정 요소 겹침 문제가 생긴다. | |
| 회원명·브랜드명·카테고리명·주소가 예상보다 길거나 공백 없이 이어질 때 줄바꿈 정책은 무엇인가? | 한국어 외 데이터와 사용자 입력은 시안의 폭 가정을 쉽게 깨뜨린다. | |
| 수량, 재고, 포인트, 쿠폰이 0·최댓값·만료 상태일 때 문구와 제어 상태는 무엇인가? | 경계값은 버튼 disabled, 오류 안내, 서버 검증 규칙에 직접 영향을 준다. | |

## 브레이크포인트와 레이아웃

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| PC와 MO 시안이 7개 대상 페이지 모두 있는가? 각 프레임의 실제 CSS px 기준 폭은 무엇인가? | 이 프로젝트는 PC/MO HTML을 분리하므로 누락 화면을 임의 추정하면 두 문서의 정보 구조가 달라진다. | |
| MO 360~767px, 태블릿 768~1023px, PC 1024px 이상이라는 현재 계약과 시안이 일치하는가? | UA 분기와 CSS 브레이크포인트가 다르면 잘못된 HTML이 전달되거나 레이아웃이 끊긴다. | |
| 768~1023px은 최대 767px MO 화면을 중앙 정렬하는 현재 정책으로 승인되는가, 별도 태블릿 시안이 있는가? | 태블릿 전용 시안 없이 임의로 2열·3열을 만들면 계약 범위를 넘어선다. | |
| PC 최소 1024px과 200%/400% 확대에서 콘텐츠 우선순위와 접힘 규칙은 무엇인가? | 넓은 시안만으로는 WCAG 1.4.10 리플로와 작은 뷰포트 대응을 결정할 수 없다. | |
| MO 하단 고정 내비게이션·구매 바·결제 바가 동시에 존재할 때 우선순위와 안전 영역은 어떻게 처리하는가? | 홈 인디케이터와 중첩된 고정 요소가 콘텐츠와 CTA를 가릴 수 있다. | |

구현 중에는 [컴포넌트 가이드의 레이아웃 미리보기](src/guide.html#layouts)에서 360/768/1024/1280px을 비교한다.

## 폰트와 텍스트

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 시스템 폰트인가, 웹폰트인가? 정확한 family·weight·style은 무엇인가? | 폰트 파일·요청 수와 글자 폭이 레이아웃 및 성능에 직접 영향을 준다. | |
| 보유 라이선스가 웹 사용(webfont), 서비스 도메인, 예상 트래픽과 파일 배포를 허용하는가? 근거 문서는 어디에 있는가? | 데스크톱 라이선스만으로 폰트를 웹 서버에 올리면 계약 위반이며 기술 작업으로 해결할 수 없다. | |
| 한글 폰트를 어떤 글리프 범위로 서브셋할 것인가? 서브셋 도구의 폐쇄망 반입과 폰트 변환 권한이 승인됐는가? | 전체 한글 폰트는 2~5MB가 될 수 있어 LCP와 데이터 사용량을 크게 악화시키며, 도구 반입 승인도 오래 걸린다. | |
| 동적 사용자명·주소·상품명에 서브셋 밖 글자가 들어오면 어떤 폴백을 쓸 것인가? | 고정 문구만 기준으로 자르면 실데이터에서 글꼴이 섞이거나 글자가 빠진다. | |
| FOUT을 허용하고 `font-display: swap`을 사용할 것인가? preload할 본문 폰트 1개는 무엇인가? | 텍스트가 안 보이는 FOIT를 피하면서도 불필요한 폰트 preload 경쟁을 막아야 한다. | |

웹폰트를 사용하기로 확정하면 바로 [CONVENTIONS.md의 웹폰트 도입 절차](CONVENTIONS.md#웹폰트-도입-절차)로 넘어간다. 파일을 전달받았다는 이유만으로 라이선스 확인보다 먼저 저장소에 넣지 않는다.

## 이미지와 미디어

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 래스터 이미지에 표시 크기의 2x 에셋이 있는가? | 고밀도 모바일 화면에서 1x 이미지는 흐리게 보인다. | |
| JPG/PNG 원본을 WebP로 변환할 수 있는가? 운영 이미지 서버도 포맷 협상을 지원하는가? | 상품 이미지가 요청 용량 대부분을 차지하므로 포맷 정책이 성능을 좌우한다. | |
| 상품·카테고리·배너 이미지의 종횡비가 각각 통일되어 있는가? 잘라내기 기준점은 어디인가? | 비율이 섞이면 CLS와 카드 높이 불일치가 생기고 사람 얼굴·상품이 잘릴 수 있다. | |
| 첫 화면 대표 이미지와 지연 로딩 대상의 구분이 무엇인가? | 모든 이미지를 eager로 받으면 LCP 자원과 경쟁하고, 대표 이미지를 lazy로 두면 LCP가 늦어진다. | |
| 의미 있는 이미지의 대체 텍스트는 누가 제공하며 장식 이미지는 어떻게 구분하는가? | 퍼블리셔가 이미지 파일명만 보고 의미를 추측해서는 정확한 대체 텍스트를 만들 수 없다. | |
| 동영상 자동재생·음소거·자막·포스터·정지 버튼 요구가 있는가? | 모바일 정책과 접근성 요구에 따라 마크업과 자산이 달라진다. | |

## 인터랙션

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 캐러셀은 자동재생하는가? 간격, 일시정지/재생 수단, 포커스·hover 시 정지 규칙은 무엇인가? | 자동 이동 콘텐츠는 읽기와 조작을 방해하며 정지 수단이 없으면 접근성 문제가 된다. | |
| 전환별 duration과 easing은 무엇인가? `prefers-reduced-motion`에서는 무엇을 제거하는가? | 시안만으로는 시간 동작을 알 수 없고 모션 감소 설정을 별도로 정의해야 한다. | |
| 탭은 선택 즉시 전환하는가, Enter/Space로 확정하는가? URL 해시·뒤로가기에 반영하는가? | 키보드 모델과 브라우저 히스토리 동작이 구현 전에 결정되어야 한다. | |
| 필터·정렬·옵션 변경은 즉시 적용인가, 적용 버튼 방식인가? 결과 갱신 뒤 포커스와 스크롤은 어디에 두는가? | 비동기 갱신이 화면만 바꾸면 키보드와 스크린리더 사용자는 결과를 놓친다. | |
| 헤더·구매 CTA·요약 영역은 sticky/fixed인가? 나타남·숨김의 스크롤 조건은 무엇인가? | 모바일 주소창과 safe area, 확대 상태에서 고정 요소가 콘텐츠를 가릴 수 있다. | |

## 접근성

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 모든 텍스트/배경 조합이 일반 텍스트 4.5:1 이상인가? 큰 텍스트 판정에 기대는 조합은 정확한 크기·굵기가 명시됐는가? | 색상 이름이나 디자이너 도구의 육안 확인만으로는 WCAG 2.1 AA를 보장할 수 없다. | |
| 할인·에러·성공·필수·품절·선택됨을 색상만으로 전달하는 곳이 없는가? | 색을 구분하지 못해도 같은 정보를 텍스트·형태로 이해할 수 있어야 한다. | |
| 아이콘만 있는 버튼의 접근 가능한 이름은 무엇인가? 장식 아이콘은 숨기는가? | 그림만으로 목적이 명확해 보여도 보조기술에는 이름이 자동으로 생기지 않는다. | |
| 제목 계층, 랜드마크, 읽기 순서가 승인됐는가? CSS로 시각 순서를 뒤집는 영역은 없는가? | 자동 검사로 디자인의 정보 우선순위와 의도한 읽기 순서를 판정할 수 없다. | |
| 오류·재고·가격·목록 결과 같은 동적 변경을 언제 어떤 문장으로 알리는가? | 화면 변화가 포커스를 이동하지 않으면 스크린리더에는 아무 일도 일어나지 않은 것처럼 보인다. | |

색 토큰의 선언된 대비는 [컴포넌트 가이드 토큰 표](src/guide.html#tokens)에서 즉시 확인할 수 있다. 다만 표는 실제 화면의 모든 중첩 배경 조합을 알지 못하므로 최종 화면도 확인한다.

## 무한 스크롤 요구

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 무한 스크롤이 꼭 필요한가, “더보기”나 페이지네이션으로 대체할 수 있는가? | 무한 로드는 푸터 도달과 현재 위치 파악을 어렵게 하며 성능 비용도 누적된다. | |
| 키보드 사용자가 새 항목을 계속 지나지 않고 푸터에 도달하는 방법은 무엇인가? | 자동으로 항목이 붙으면 Tab과 문서 탐색으로 푸터에 영원히 도달하지 못할 수 있다. | |
| 상세 페이지에서 뒤로 왔을 때 이전 목록 위치와 이미 로드한 항목을 어떻게 복원하는가? | 위치 복원이 없으면 사용자가 수십 개 상품을 다시 탐색해야 한다. | |
| 로딩 시작·완료·끝·실패를 어떻게 알리고 취소/재시도하는가? | 화면 밖 로딩과 실패는 보조기술 및 느린 네트워크에서 인지하기 어렵다. | |
| URL에 페이지/커서 상태를 남기는가? 공유·새로고침 결과는 무엇인가? | 브라우저 기본 탐색과 분석 데이터가 무한 스크롤 상태를 재현할 수 있어야 한다. | |

## 계약과 QA 범위

| 확인 질문 | 왜 물어야 하는가 | 답/근거 |
|---|---|---|
| 최소·최대 대상 해상도와 테스트할 실기기/OS/브라우저 버전은 무엇인가? | “모바일/PC”만으로는 재현 가능한 합격 기준이 되지 않는다. 현재 기본 대상은 iOS Safari와 Android Chrome 최신 2개 버전이다. | |
| 퍼블리싱 범위에 실제 API 연동, 결제 로직, 서버 템플릿 이관 중 무엇이 포함되는가? | 이 보일러플레이트의 `data-bind*`는 연동 지점 표시이며 도메인 로직 구현이 아니다. | |
| 1차 QA 일정, 수정 요청 반영 횟수, 회차별 기간과 종료 조건은 무엇인가? | 횟수와 기간이 없으면 디자인 변경과 결함 수정의 경계가 흐려져 일정이 무한히 늘어난다. | |
| QA 중 새 기능·새 브레이크포인트·새 상태 추가는 변경 요청으로 처리하는가? | 누락 시안 보완과 최초 범위 밖 추가 작업을 같은 결함으로 계산하지 않기 위해서다. | |
| 접근성·크로스브라우징 결함의 우선순위와 출시 차단 기준은 무엇인가? | 발견만 하고 출시 판단을 미루면 심각한 결함이 그대로 배포될 수 있다. | |

## 검토 완료 조건

- 모든 빈칸에 답, 담당자, Figma/이슈 근거가 있다.
- PC/MO와 상태·빈 화면·극단값의 누락 목록이 합의됐다.
- 웹폰트 라이선스와 서브셋, 이미지 포맷과 비율, 인터랙션 시간이 확정됐다.
- 대상 해상도·실기기와 QA 수정 횟수·기간이 계약 범위로 남았다.
- 미확정 항목은 임의 구현하지 않고 결정일과 작업 영향도를 가진 이슈로 등록됐다.
