# 크로스브라우징 대응표

대상은 iOS Safari와 Android Chrome 최신 2개 버전이다. “최신”은 계속 바뀌므로 QA마다 테스트한 기기·OS·브라우저의 정확한 버전과 날짜를 [QA_CHECKLIST.md](QA_CHECKLIST.md)에 기록한다. 에뮬레이터 확인만으로 완료하지 않고 최소 한 대씩 실기기에서 입력, 키보드, safe area, 주소창 변화를 확인한다.

아래 표는 현재 `src/assets/scss/base/_reset.scss`와 관련 공통 스타일에 실제로 들어 있는 대응이다.

## 공통 대응

| 증상 | 원인 | 프로젝트 해결 | 확인 방법 |
|---|---|---|---|
| 입력 포커스 시 화면이 갑자기 확대됨 | iOS Safari는 입력 글자가 16px 미만이면 가독성을 위해 자동 확대한다. | 리셋에서 `input`, `select`, `textarea`를 `font-size: 16px`로 고정한다. 컴포넌트에서 더 작은 값으로 덮지 않는다. | iPhone에서 각 입력을 탭하고 viewport 배율이 바뀌지 않는지 본다. |
| 하단 고정 요소가 홈 인디케이터에 가림 | 노치 기기의 안전 영역은 일반 viewport 안쪽에 포함된다. | 리셋에서 `--safe-top/right/bottom/left: env(safe-area-inset-*, 0px)`를 정의하고 하단 내비게이션·결제 바·모달 등이 이를 padding에 더한다. 각 HTML의 viewport 메타에 `viewport-fit=cover`를 유지한다. | 홈 버튼 없는 iPhone 세로/가로에서 CTA와 닫기 버튼을 눌러 본다. |
| 상단 헤더나 전체 화면 모달이 노치에 겹침 | `viewport-fit=cover` 사용 시 콘텐츠가 안전 영역까지 확장된다. | 헤더·GNB·모달이 `--safe-top`, `--safe-left`, `--safe-right`를 소비한다. | 세로/가로 회전 후 제목·닫기 버튼의 여백을 확인한다. |
| `100vh` 영역이 화면보다 크거나 주소창 뒤로 들어감 | 모바일 브라우저의 주소창이 접히고 펼쳐지며 고정 viewport 높이와 실제 보이는 높이가 달라진다. | `body`에 `min-height: 100vh` 폴백을 먼저 쓰고 `min-height: 100dvh`를 뒤에 둔다. GNB·모달도 `100dvh`를 사용한다. | 주소창을 펼친 상태와 접은 상태에서 페이지 끝과 모달 버튼을 확인한다. |
| 탭할 때 회색 하이라이트 박스가 디자인과 충돌함 | iOS/Android 브라우저의 기본 tap highlight가 적용된다. | `html`에 `-webkit-tap-highlight-color: transparent`를 적용한다. 대신 버튼의 `:active`, 상태 텍스트와 키보드 `:focus-visible` 피드백은 유지한다. | 링크·버튼을 길게/짧게 탭하고 피드백이 완전히 사라지지 않았는지 함께 확인한다. |
| 회전·접근성 글자 설정 뒤 텍스트가 임의로 커짐 | WebKit의 자동 text size adjustment가 레이아웃에 개입한다. | `html`에 `-webkit-text-size-adjust: 100%`를 적용해 작성한 크기 체계를 유지한다. | iPhone 회전 전후에 제목·입력·버튼의 상대 크기와 잘림을 본다. |
| 이미지·SVG·동영상이 부모 폭을 넘어 가로 스크롤을 만듦 | 대체 요소의 원본 크기가 좁은 viewport보다 클 수 있다. | 리셋에서 `img`, `picture`, `svg`, `video`, `canvas`를 block으로 두고 `max-width: 100%`, 이미지·비디오는 `height: auto`를 적용한다. | 360px 및 320 CSS px 상당에서 큰 미디어가 페이지 전체를 밀지 않는지 본다. |
| 컴포넌트별 박스 크기가 브라우저마다 다르게 계산됨 | 기본 `content-box`에서는 padding과 border가 선언 폭 밖에 더해진다. | 모든 요소와 가상 요소에 `box-sizing: border-box`를 적용한다. | 고정 폭 입력·버튼과 100% 폭 요소가 부모를 넘지 않는지 비교한다. |
| 폼 컨트롤 글꼴·색이 주변 텍스트와 다름 | 네이티브 컨트롤은 브라우저 기본 폰트와 색을 가질 수 있다. | `button`, `input`, `select`, `textarea`에 `font: inherit; color: inherit`를 적용한다. | iOS/Android에서 입력과 버튼의 글꼴·굵기를 본문과 비교한다. |
| 모션 감소 설정인데 전환·스크롤 애니메이션이 계속됨 | 사용자 설정은 CSS를 별도로 처리하지 않으면 자동으로 모든 애니메이션을 끄지 않는다. | `prefers-reduced-motion: reduce`에서 animation·transition을 제거하고 scroll behavior를 `auto`로 바꾼다. | 두 OS에서 모션 감소를 켜고 모달·탭·레이아웃 전환을 확인한다. |
| 모바일 가로 탭 스크롤이 뻣뻣하거나 스크롤바가 디자인을 가림 | 수평 overflow와 스크롤 UI 처리가 엔진마다 다르다. | 탭 목록에 `overflow-x: auto`, `-webkit-overflow-scrolling: touch`, 엔진별 스크롤바 숨김을 적용한다. | 항목이 많은 탭을 손가락으로 끝까지 스크롤하고 키보드 포커스 항목도 화면 안으로 오는지 본다. |

## 구현 시 주의

- `--safe-*` 변수만 정의해도 해결되는 것은 아니다. 새 fixed/sticky 요소의 실제 padding·offset에 해당 변수를 사용하고, 본문 마지막 항목이 요소 뒤에 가리지 않도록 여백도 확보한다.
- `env(safe-area-inset-*)`는 `viewport-fit=cover`가 빠지면 0으로 돌아올 수 있다. 새 PC/MO 페이지의 viewport 메타를 복사할 때 이 값을 누락하지 않는다.
- `100dvh` 앞의 `100vh`는 구형 엔진 폴백이므로 순서를 바꾸지 않는다. 전체 화면 레이어는 주소창, 회전, 소프트 키보드가 열린 세 상태를 모두 확인한다.
- tap highlight를 없앤 뒤 `:active`와 `:focus-visible`까지 제거하지 않는다. 터치와 키보드 피드백은 별개다.
- 네이티브 select, 날짜/숫자 입력, 자동완성, 비밀번호 관리자의 실제 UI는 CSS만으로 같게 만들 수 없다. 외형 일치보다 읽기·입력·오류 복구 가능 여부를 우선한다.
- PC/MO HTML은 분리되어도 공통 SCSS·JavaScript는 공유한다. 브라우저 버그를 한쪽 엔트리에만 우회하지 말고 공통 계층에서 해결하되, 플랫폼 분기가 꼭 필요하면 원인과 제거 조건을 주석과 아래 이력에 남긴다.

## 기능별 실기기 매트릭스

| 기능 | iOS Safari | Android Chrome |
|---|---|---|
| 입력 focus·자동 확대·소프트 키보드 | 실제 input/select/textarea와 주문 폼 제출까지 확인 | 실제 input/select/textarea와 자동완성까지 확인 |
| safe area·회전 | 노치/홈 인디케이터 기기에서 세로·가로 확인 | gesture navigation과 버튼 navigation 환경 중 가능한 범위 기록 |
| 주소창·`dvh` | 주소창 펼침/접힘, 전체 화면 모달 확인 | 상단/하단 브라우저 UI 변화와 전체 화면 모달 확인 |
| 터치·가로 스크롤 | 탭, 이미지 갤러리, 바텀시트 확인 | 탭, 이미지 갤러리, 바텀시트 확인 |
| 뒤로가기·스크롤 복원 | 상세→목록, 열린 레이어, 폼 입력 상태 확인 | 상세→목록, 열린 레이어, 폼 입력 상태 확인 |
| 모션 감소 | iOS 설정을 켜고 전환 확인 | Android 접근성 설정을 켜고 전환 확인 |

## 이슈 이력

이 문서는 고정된 지원표가 아니라 실제 작업에서 발견한 문제를 계속 추가하는 운영 문서다. 브라우저 이슈를 발견하면 아래 형식으로 표 아래에 추가한다. 해결 CSS만 적지 말고 재현 조건과 제거 기준을 남겨 오래된 우회가 영구화되지 않게 한다.

| 발견일 | 환경 | 증상·재현 조건 | 원인 | 해결/우회 | 회귀 확인 | 제거 조건 |
|---|---|---|---|---|---|---|
| YYYY-MM-DD | 기기 / OS / 브라우저 버전 | 페이지와 단계 | 확인된 엔진/레이아웃 원인 | 적용 파일과 방식 | 확인한 페이지·버전 | 업스트림 수정 또는 지원 버전 변경 |
