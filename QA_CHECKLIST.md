# QA 체크리스트

이 문서는 자동 검사로 판정하기 어려운 실제 동작·시각 순서·콘텐츠 극단값·실기기 문제를 사람이 확인하기 위한 것이다. QA 시작 전 `node tools/build.js`, `node tools/check.js`를 통과시키고 [컴포넌트 가이드의 axe 결과](src/guide.html#accessibility)를 확인한다. 아래 항목은 그 자동 검사를 반복하는 목록이 아니라 자동화의 빈틈을 검증한다.

## 기록 정보

- 빌드/커밋:
- 검사 일시·담당자:
- 환경(기기, OS, 브라우저 정확한 버전):
- 뷰포트/확대율:
- 테스트 계정·데이터 조건:
- 관련 Figma/요구사항:

결함은 `페이지 / 환경 / 사전 조건 / 재현 절차 / 실제 결과 / 기대 결과 / 스크린샷·영상 / 심각도`를 함께 기록한다. “모바일에서 깨짐”처럼 재현할 수 없는 문장만 남기지 않는다.

## 키보드

- [ ] 페이지를 새로 연 뒤 Tab만으로 헤더부터 본문, 푸터까지 모든 링크·버튼·입력·탭·아코디언·수량 제어에 도달한다.
- [ ] Shift+Tab으로 역순 이동해도 빠지거나 반복되는 구간이 없다.
- [ ] 포커스 표시가 배경과 상태에 관계없이 분명히 보이고, sticky/fixed 요소나 스크롤 컨테이너 뒤에 가려지지 않는다.
- [ ] 포커스 순서가 화면의 읽기 순서와 일치한다. CSS의 `order`, grid 배치, absolute 요소 때문에 좌우·상하 순서가 튀지 않는다.
- [ ] Enter와 Space가 요소의 네이티브 역할에 맞게 동작하며, 클릭 전용 기능이 없다.
- [ ] 가로 탭은 좌우 방향키·Home·End로 이동하고 선택 상태와 표시 패널이 일치한다.
- [ ] 아코디언을 열고 닫을 때 포커스가 트리거에 유지되고 `aria-expanded`와 보이는 내용이 일치한다.
- [ ] 모달을 열면 포커스가 모달 내부로 들어가고 Tab/Shift+Tab이 내부에서 순환한다.
- [ ] 모달은 Esc와 명시적 닫기 버튼으로 닫히며, 닫힌 뒤 포커스가 모달을 연 요소로 돌아간다. 중첩 모달도 한 단계씩 복귀한다.
- [ ] 모달 뒤 배경은 포커스·스크롤·보조기술 탐색 대상이 되지 않는다.
- [ ] 폼 제출 실패 시 사용자가 첫 오류와 오류 요약에 도달할 수 있고 입력 내용을 잃지 않는다.

## 확대와 리플로

- [ ] PC 브라우저 200% 확대에서 텍스트·버튼·입력·가격·고정 헤더가 겹치거나 잘리지 않는다.
- [ ] PC 브라우저 400% 확대에서 핵심 기능과 콘텐츠가 사라지지 않고 한 축의 자연스러운 읽기 흐름을 유지한다.
- [ ] 320 CSS px 상당 뷰포트에서 페이지 전체 가로 스크롤이 생기지 않는다. 표·코드·이미지처럼 본질적으로 넓은 콘텐츠는 해당 영역 안에서만 스크롤된다.
- [ ] 확대 상태에서 sticky/fixed 헤더, 하단 내비게이션, 구매·결제 바가 화면 대부분을 가리지 않으며 마지막 콘텐츠까지 스크롤할 수 있다.
- [ ] 텍스트 크기만 200%로 키워도 라벨, 오류 문구, 버튼명이 잘리거나 의미 없는 아이콘만 남지 않는다.
- [ ] 가로·세로 회전 뒤 현재 위치, 열린 모달, 입력값이 불필요하게 초기화되지 않는다.

## 색상과 시각 정보

- [ ] 화면을 흑백/그레이스케일로 바꿔도 오류·성공·필수·품절·할인·선택됨을 텍스트, 아이콘 형태, 선 등으로 구분할 수 있다.
- [ ] hover에서만 보이는 정보가 없으며 터치·키보드 사용자도 같은 내용을 얻는다.
- [ ] 포커스와 선택 상태가 색 변화 하나에만 의존하지 않는다.
- [ ] 링크가 본문 안에서 색 외의 단서 또는 충분히 명확한 문맥으로 식별된다.
- [ ] disabled 요소와 활성 요소가 구분되면서도 필요한 안내 문구는 읽을 수 있다.

## 콘텐츠 극단값과 실패

- [ ] 매우 긴 상품명·브랜드명·카테고리명·회원명에서 주변 가격과 버튼이 밀려나지 않고, 말줄임된 정보에 접근 가능한 전체 이름이 필요한지 확인한다.
- [ ] 8자리 이상 금액, 큰 할인율, 정가·판매가·단위가 함께 있어도 줄바꿈 순서와 금액 의미가 유지된다.
- [ ] 리뷰 0개를 평점 0점과 혼동하지 않고 “아직 리뷰 없음” 상태와 다음 행동이 보인다.
- [ ] 옵션 20개와 긴 옵션명에서도 마지막 항목, 닫기, 구매 CTA에 도달할 수 있다.
- [ ] 목록 0개일 때 검색·장바구니·주문·리뷰·쿠폰 각각의 빈 상태와 적절한 다음 행동이 나타난다.
- [ ] 이미지 URL 실패·느린 로드에서 레이아웃이 크게 움직이지 않고 대체 텍스트나 대체 이미지가 목적을 전달한다.
- [ ] 공백 없는 긴 영문/URL, 한글·영문·숫자 혼합, 이모지 등 실제 입력에서 overflow가 생기지 않는다.
- [ ] 수량·재고·포인트·쿠폰의 0과 최댓값에서 제어 상태, 문구, 서버 응답이 일치한다.

## 로딩·빈 상태·오류 상태

- [ ] 상품 목록, 검색 결과, 장바구니, 주문 목록, 리뷰, 쿠폰 등 모든 목록형 영역에 로딩·빈·오류 상태가 각각 있다.
- [ ] 로딩 스켈레톤이 실제 콘텐츠와 비슷한 크기를 예약해 로드 완료 시 큰 이동이 없다.
- [ ] 느린 응답에서 로딩 상태가 중복 submit을 막고 처리 중임을 보이는 문구로 알린다.
- [ ] 부분 실패와 전체 실패가 구분된다. 예: 상품 이미지만 실패해도 나머지 상품 정보와 구매 경로는 유지된다.
- [ ] 오류에는 원인 또는 사용자가 할 수 있는 다음 행동과 재시도 수단이 있다.
- [ ] 재시도 후 성공하면 이전 오류가 시각적으로도, 보조기술 상태에서도 제거된다.
- [ ] 비동기 목록 갱신 뒤 결과 개수와 완료 상태가 알림으로 전달되되 같은 메시지를 과도하게 반복하지 않는다.

## 스크린리더와 의미 구조

- [ ] VoiceOver 또는 TalkBack으로 페이지 제목, 랜드마크, heading 목록을 탐색했을 때 시각적 정보 구조와 순서가 일치한다.
- [ ] 아이콘 버튼, 이미지 링크, 가격, 할인, 별점, 주문 단계에 문맥 없는 “버튼”, 파일명, 중복 이름이 읽히지 않는다.
- [ ] 입력 레이블·필수 여부·도움말·오류가 한 필드의 정보로 함께 읽힌다.
- [ ] 찜, 필터, 탭, 아코디언, 수량, 모달의 현재 상태가 조작 직후 갱신되어 읽힌다.
- [ ] 토스트·서버 오류·재고 변경·검색 결과 갱신은 중요한 정도에 맞게 알리고, 사용자의 현재 발화를 불필요하게 끊지 않는다.
- [ ] 장식 SVG와 중복 이미지가 읽기 순서에 끼지 않고, 의미 있는 이미지에는 화면 문맥에 맞는 대체 텍스트가 있다.

## 실기기

- [ ] iOS Safari에서 상단 노치와 하단 홈 인디케이터가 헤더·하단 내비게이션·구매/결제 CTA·모달 버튼을 가리지 않는다.
- [ ] iOS Safari에서 input, select, textarea에 포커스해도 화면이 자동 확대되지 않는다.
- [ ] iOS Safari 주소창이 접히고 펼쳐질 때 `100dvh` 영역, 전체 화면 모달, GNB 높이가 잘못 남지 않는다.
- [ ] 소프트 키보드가 열린 상태에서 현재 입력과 오류 문구, 다음/제출 버튼으로 스크롤할 수 있다.
- [ ] Android Chrome에서 뒤로가기가 모달·필터·상세→목록의 기대 상태와 스크롤 위치를 유지한다.
- [ ] iOS/Android 모두 빠른 연속 탭, 스와이프, 화면 회전 뒤 이중 제출·스크롤 잠금 잔존·닫히지 않는 레이어가 없다.
- [ ] 터치 대상은 실제 손가락으로 구분 가능하며 44×44px 수준의 영역과 충분한 간격을 갖는다.
- [ ] 느린 4G 또는 네트워크 throttling에서 시스템 폰트/웹폰트, 대표 이미지, 목록 이미지의 로드 우선순위가 의도대로 보인다.

## 페이지별 점검

### 메인

- [ ] 대표 배너의 링크 목적과 이미지 대체 텍스트가 기획전 내용을 정확히 전달한다.
- [ ] 캐러셀이 있다면 자동재생 정지, 이전/다음, 현재 슬라이드, 키보드·스와이프 동작이 일치한다.
- [ ] 추천·신상품 등 여러 상품 섹션의 heading과 목록 이름을 구분할 수 있다.
- [ ] 첫 화면 대표 이미지와 아래 상품 이미지의 eager/lazy 우선순위가 올바르다.

### 카테고리

- [ ] 하위 카테고리 이름이 길거나 개수가 늘어도 순서와 터치 영역이 유지된다.
- [ ] 현재 카테고리, breadcrumb, 페이지 제목이 같은 위치를 가리킨다.
- [ ] 카테고리 배너와 추천 상품 사이의 heading 구조 및 링크 목적이 명확하다.
- [ ] PC/MO에서 카테고리 정보와 상품 진입 경로가 누락 없이 대응한다.

### 상품 목록

- [ ] 필터를 키보드와 터치로 적용·해제할 수 있고 선택 개수, 결과 개수, 초기화 상태가 일치한다.
- [ ] 정렬 변경과 필터 적용 뒤 결과 갱신을 알리고 포커스·스크롤이 예측 가능한 위치에 남는다.
- [ ] 필터 결과 0개, 서버 오류, 로딩, 마지막 페이지 상태가 서로 구분된다.
- [ ] 페이지네이션 또는 더보기 후 뒤로가기에서 이전 페이지·스크롤 위치를 복원한다.
- [ ] 상품 카드의 긴 이름, 품절, 할인, 찜 상태가 한 목록 안에서 함께 나타나도 정렬과 접근 가능한 이름이 유지된다.

### 상품 상세

- [ ] 대표 이미지, 썸네일, 확대 모달을 키보드·터치로 조작하고 현재 이미지와 대체 텍스트가 일치한다.
- [ ] 옵션 미선택·품절·20개 이상·긴 이름, 수량 최소/최대, 재고 변경 상태가 구매 CTA와 일치한다.
- [ ] 정가·할인가·할인율·배송 정보가 큰 금액에서도 의미 순서대로 읽힌다.
- [ ] 상세/리뷰/배송 탭 또는 앵커 이동 뒤 heading, 포커스, URL 해시가 기대대로 동작한다.
- [ ] MO 하단 구매 바와 옵션 바텀시트가 홈 인디케이터·키보드에 가리지 않고 닫은 뒤 트리거로 복귀한다.

### 장바구니

- [ ] 전체 선택, 개별 선택, 품절 상품 제외 규칙과 선택 금액 계산이 일치한다.
- [ ] 수량 최소/최대와 재고 부족 응답이 행 금액·총액에 즉시 반영되고 알림으로 전달된다.
- [ ] 개별/선택 삭제 모달의 대상과 문구가 정확하며 취소·확인 뒤 포커스가 적절한 위치로 돌아간다.
- [ ] 빈 장바구니에서 요약·결제 바가 남지 않고 쇼핑 계속하기 경로가 보인다.
- [ ] MO 고정 결제 바가 마지막 상품과 배송 안내를 가리지 않는다.

### 주문·결제

- [ ] 저장 배송지/신규 배송지 전환 시 관련 필드의 값·필수·disabled 상태가 정확하다.
- [ ] 우편번호 검색 모달, 주소 결과 선택, 닫기 뒤 포커스와 입력값이 유지된다.
- [ ] 배송 방법, 쿠폰/포인트, 결제 수단을 바꿀 때 최종 금액과 할인 설명이 일치한다.
- [ ] 필수 약관 전체 동의와 개별 동의가 양방향으로 동기화되고 선택 약관을 구분한다.
- [ ] 빈 필드·잘못된 형식·서버 오류 후 첫 오류로 이동하고 입력값과 선택을 잃지 않는다.
- [ ] 결제 버튼 연속 조작이 중복 주문을 만들지 않으며 로딩·성공·실패 후 상태가 명확하다.

### 마이페이지

- [ ] 로그인 전 안내와 로그인 후 회원 정보가 동시에 노출되지 않고, 상태 전환 시 heading 구조가 유지된다.
- [ ] 회원 등급·포인트·쿠폰 수가 0 또는 큰 값이어도 링크와 레이아웃이 유지된다.
- [ ] 최근 주문의 주문번호·날짜·상태·상품과 배송조회/상세 링크의 대상이 일치한다.
- [ ] 주문 내역이 없을 때 빈 상태와 쇼핑 이동 CTA가 보이고 목록 landmark만 빈 채 남지 않는다.
- [ ] 여러 메뉴 그룹을 스크린리더와 키보드로 구분하고 현재 위치를 확인할 수 있다.

## 완료 조건

- [ ] iOS Safari와 Android Chrome 최신 2개 버전의 실제 버전·기기 기록이 있다.
- [ ] 360, 768, 1024, 1280px과 200%/400% 확대 결과가 기록됐다.
- [ ] 7개 페이지의 PC/MO, 주요 상태와 극단값을 확인했다.
- [ ] 출시 차단 결함은 모두 수정·재검증됐고, 보류 결함은 담당자·기한·승인 근거가 있다.
